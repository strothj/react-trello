var React = require('react');
var ReactDOM = require('react-dom');

var Card = function() {
    return (
        <div className="card">
            This is a card
        </div>
    );
};

module.exports = Card;
